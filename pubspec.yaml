name: the_super_test
description: The Super Test - Elevate your focus. Master your future.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get_it: ^9.2.0
  dio: ^5.9.1
  flutter_secure_storage: ^10.0.0
  dio_cache_interceptor: ^4.0.5
  http_cache_hive_store: ^5.0.2
  device_info_plus: ^12.3.0
  package_info_plus: ^9.0.0
  intl: ^0.20.2
  freezed_annotation: ^3.1.0
  fpdart: ^1.2.0
  flutter_bloc: ^9.1.1
  go_router: ^14.6.2
  nested: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  freezed:
  json_serializable:
  build_runner:
  json_annotation:
  mocktail: ^1.0.4
  bloc_test:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
